// ... existing code ...
    }
}

export class ConnectionManager {
    constructor(scene, config = TendrilConstants) {
        this.scene = scene;
        this.config = config;
// ... existing code ...

